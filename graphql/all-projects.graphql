query MainProjects {
  allProjects(orderBy: position_ASC, first: 2) {
    id
    slug
    title
    headline
    teaserImage {
      responsiveImage(imgixParams: { fit: crop, w: 720, h: 480, auto: format }) {
        ...responsiveImage
      }
    }
  }
}

query OtherProjects {
  allProjects(orderBy: position_ASC, skip: 2) {
    id
    slug
    title
    headline
    teaserImage {
      responsiveImage(imgixParams: { fit: crop, w: 460, h: 240, auto: format }) {
        ...responsiveImage
      }
    }
  }
}
