fragment teaserCard on TeaserCardRecord {
  id
  eyebrow
  title
  text
  color
  linkLabel
  url
  newTab
  teaserCardImage {
    responsiveImage(imgixParams: { fit: crop, w: 460, h: 240 }) {
      ...responsiveImage
    }
  }
  isExternalUrl
  link {
    ... on PageRecord {
      slug
    }
    ... on ProjectRecord {
      slug
    }
    ... on OfferRecord {
      slug
    }
    ... on BlogpostRecord {
      slug
    }
    ... on TopicRecord {
      slug
    }
  }
}
