fragment pageRecordLink on PageRecord {
  id
  slug
  parent {
    id
    slug
    parent {
      id
      slug
      parent {
        id
        slug
        parent {
          id
          slug
        }
      }
    }
  }
}
