fragment contactBlock on ContactRecord {
  id
  text
  contact {
    imagePortrait {
      responsiveImage(imgixParams: { fit: crop, w: 208, h: 208, auto: format }) {
        ...responsiveImage
      }
    }
    name
    email
    tel
    booking
  }
}
