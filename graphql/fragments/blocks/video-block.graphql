fragment videoBlock on VideoRecord {
  id
  videoType
  video {
    url
    provider
    providerUid
    title
  }
  videoFile {
    video {
      ...videoFragment
    }
  }
  controls
  loop
  autoplay
  maxWidth
  disableMarginTop
  disableMarginBottom
}
