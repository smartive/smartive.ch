fragment offers on OfferRecord {
  id
  slug
  title
  description
  timespan
  linkLabel
  color
  offerImage {
    responsiveImage(imgixParams: { fit: crop, w: 460, h: 240, auto: format }) {
      ...responsiveImage
    }
  }
}
