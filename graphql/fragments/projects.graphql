fragment projects on ProjectRecord {
  id
  slug
  title
  headline
  teaserImage {
    responsiveImage(imgixParams: { fit: crop, w: 460, h: 240, auto: format }) {
      ...responsiveImage
    }
  }
}
